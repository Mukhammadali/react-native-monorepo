{
  "name": "dev",
  "version": "0.0.1",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "apps/*"
    ]
  },
  "scripts": {
    "android": "yarn workspace @dev/mobile android",
    "compile": "tsc -b --incremental",
    "ios": "yarn workspace @dev/mobile ios",
    "web": "yarn workspace @dev/web start",
    "studio": "yarn workspace @dev/mobile studio",
    "xcode": "yarn workspace @dev/mobile xcode"
  },
  "dependencies": {
    "react-native": "*"
  },
  "devDependencies": {
    "typescript": "3.7.5"
  }
}
